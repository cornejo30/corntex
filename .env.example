PORT=3000
HOST_PORT=3000
BASE_URL=http://localhost:3000

# Authentication
# Change this to a secure random string in production
JWT_SECRET=your_super_secret_jwt_key_here

# Database
# The path is relative to the project root inside the container
DB_PATH=shorten.db
